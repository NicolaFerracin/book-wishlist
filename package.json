{
  "name": "book-wishlist",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "import:amazon": "tsx --tsconfig server/tsconfig.json scripts/import-amazon.ts",
    "scrape:prices": "tsx --tsconfig server/tsconfig.json scripts/scrape-all-prices.ts"
  },
  "devDependencies": {
    "cheerio": "^1.0.0",
    "concurrently": "^9.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
